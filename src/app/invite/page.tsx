import { InviteAccept } from '@/components/sections/InviteAccept'

export const metadata = {
  title: 'Accept Project Invitation — ClarityOS',
  description: 'Accept your project invitation and join your client dashboard.',
}

export default function InvitePage() {
  return <InviteAccept />
}
